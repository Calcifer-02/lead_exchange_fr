# Lead Exchange Frontend

Платформа для обмена лидами между агентствами недвижимости.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pnpm install
```

### Запуск в режиме разработки

```bash
cd packages/frontend
pnpm dev
```

Приложение будет доступно на `http://localhost:5173`

### Сборка для production

```bash
cd packages/frontend
pnpm build
```

## 🏗️ Архитектура

- **Frontend:** React + TypeScript + Vite + Ant Design
- **Backend:** Развернут на Render.com (`https://lead-exchange.onrender.com`)
- **API:** RESTful API с JWT авторизацией

## 📦 Структура проекта

```
lead_exchange_fr/
├── packages/
│   ├── frontend/           # React приложение
│   │   ├── src/
│   │   │   ├── api/       # API клиенты
│   │   │   ├── components/# React компоненты
│   │   │   ├── pages/     # Страницы приложения
│   │   │   ├── hooks/     # Custom hooks
│   │   │   ├── types/     # TypeScript типы
│   │   │   └── utils/     # Утилиты
│   │   └── package.json
│   └── shared/            # Общие типы и утилиты
└── package.json           # Workspace root
```

## 🔧 Конфигурация

### Переменные окружения

Создайте файл `.env` в папке `packages/frontend/`:

```env
# API Configuration
VITE_API_BASE_URL=https://lead-exchange.onrender.com

# MinIO Configuration
VITE_MINIO_ENDPOINT=http://localhost:9000
VITE_MINIO_BUCKET=photos
```

## 🐳 Docker

Запуск через Docker Compose:

```bash
docker-compose up
```

Frontend будет доступен на `http://localhost:3000`

## 🛠️ Технологии

### Frontend
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик
- **Ant Design** - UI компоненты
- **React Router** - Роутинг
- **Axios** - HTTP клиент
- **TanStack Query** - Управление серверным состоянием

### Backend
- Node.js + Express
- PostgreSQL
- JWT Authentication
- Развернут на Render.com

## 📝 Основные функции

- 🔐 Авторизация и регистрация пользователей
- 📊 Dashboard с аналитикой
- 📋 Каталог лидов с фильтрацией
- 🤝 Создание и управление сделками
- 🏢 Управление объектами недвижимости
- 🤖 ML-подбор подходящих объектов для лидов
- 💰 Управление финансами и кредитами
- 📸 Загрузка изображений

## 📚 API Endpoints

Базовый URL: `https://lead-exchange.onrender.com`

### Авторизация
- `POST /v1/auth/register` - Регистрация
- `POST /v1/auth/login` - Вход

### Лиды
- `GET /v1/leads` - Список лидов
- `GET /v1/leads/:id` - Детали лида
- `POST /v1/leads` - Создать лид
- `PUT /v1/leads/:id` - Обновить лид

### Сделки
- `GET /v1/deals` - Список сделок
- `GET /v1/deals/:id` - Детали сделки
- `POST /v1/deals` - Создать сделку
- `PUT /v1/deals/:id` - Обновить сделку

### Файлы
- `POST /v1/files/upload` - Загрузить файл
- `POST /v1/files/upload-multiple` - Загрузить несколько файлов

## 🔄 Последние изменения

Backend перенесен на Render.com. Подробности миграции см. в [MIGRATION_TO_RENDER.md](./MIGRATION_TO_RENDER.md)

## 👨‍💻 Разработка

### Установка зависимостей
```bash
pnpm install
```

### Линтинг
```bash
pnpm lint
```

### Сборка
```bash
pnpm build
```

## 📄 Лицензия

Private project

## 🤝 Контакты

Для вопросов и предложений обращайтесь к команде разработки.

