version: "3.8"
services:
  frontend:
    build: ./packages/frontend
    ports:
      - "3000:5173"
    volumes:
      - ./packages/frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - VITE_API_BASE_URL=https://lead-exchange.onrender.com
